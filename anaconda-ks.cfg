#version=DEVEL
# System authorization information
auth --enableshadow --passalgo=sha512
# Use CDROM installation media
cdrom
# Use graphical install
graphical
# Run the Setup Agent on first boot
firstboot --enable
ignoredisk --only-use=sda
# Keyboard layouts
keyboard --vckeymap=cn --xlayouts='cn'
# System language
lang zh_CN.UTF-8

# Network information
network  --bootproto=dhcp --device=ens33 --onboot=off --ipv6=auto --no-activate
network  --hostname=localhost.localdomain

# Root password
rootpw --iscrypted $6$HjC4Tu3917yZd2b.$Fzx2q1BIQhcaqtcskUOO53HYNRURHv3iC6TBsld1TUtw3PIbZNCfuk2uzcTJoEPnAkN/RpjOUHmfMJfouz3tK.
# System services
services --enabled="chronyd"
# System timezone
timezone Asia/Shanghai --isUtc
# System bootloader configuration
bootloader --append=" crashkernel=auto" --location=mbr --boot-drive=sda
# Partition clearing information
clearpart --none --initlabel
# Disk partitioning information
part pv.253 --fstype="lvmpv" --ondisk=sda --size=18031
part /boot --fstype="xfs" --ondisk=sda --size=2448
volgroup centos --pesize=4096 pv.253
logvol swap  --fstype="swap" --size=2448 --name=swap --vgname=centos
logvol /  --fstype="xfs" --size=15575 --name=root --vgname=centos

%packages
@^virtualization-host-environment
@base
@core
@virtualization-hypervisor
@virtualization-tools
chrony
kexec-tools

%end

%addon com_redhat_kdump --enable --reserve-mb='auto'

%end

%anaconda
pwpolicy root --minlen=6 --minquality=1 --notstrict --nochanges --notempty
pwpolicy user --minlen=6 --minquality=1 --notstrict --nochanges --emptyok
pwpolicy luks --minlen=6 --minquality=1 --notstrict --nochanges --notempty
%end
